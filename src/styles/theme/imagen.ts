import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    paper: {
      paperChannel: 'rgb(255 255 255)', // Establece el canal de papel como blanco (RGB)
    },
  },
});
